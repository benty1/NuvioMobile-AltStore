name: Update AltStore Repo

on:
  schedule:
    - cron: '*/5 * * * *' # Runs every 5 minutes (maximum frequency)
  workflow_dispatch: # Allows manual triggers from the Actions tab

jobs:
  update-repo:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Required to commit and push updated repo.json
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Run dynamic update script
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python update_repo.py

      - name: Commit and push if repo.json changed
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add repo.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update repo.json with latest releases [skip ci]" && git push)
